server:
  port: ${port:8082}
# 连接数据库的配置
spring:
  datasource:
    url: jdbc:mysql://176.17.100.105:3306/mall_ams?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
# 日志的显示级别
logging:
  level:
    com.syt.passport: trace
  file:
    name: log/my.log
